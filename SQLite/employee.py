class Employee:

        def __init__(self, first, last, pay, title, id):
                self.first = first
                self.last = last
                self.pay = pay
                self.id = id
                self.title = title